1. Add a Toolbox tab in sidebar
	In toolbox page
		Message formatter: Allows to format messages with Minecraft MOTD formatting
		There should be two panes: Left pane is for Input and right pane for formatted output
		Top of the two panes should be a wide toolbar
		The toolbar will have formatting buttons: bold, italic, underline, etc and a color picker button, which when clicked will open a dialog with valid motd formatting colors.
		The right pane should show the formatted text in 30px inconsolata font.

2. Add a Quick Macros tab
	Before it: Implement function helpers to allow sending commands to the minecraft server through the flask api (which should already be there, but implement quick wrappers for easy sending of commands).

	Inside it, you can add macros to quick control buttons, which will show in a grid.
	in the page-header section, right aligned, add New macro button
	when clicked, will open a dialog, with fields:
	1. Macro icon (show some common bootstrap icons)
	2. Macro title
	3. Macro command

3. Show information about each property in server properties edit dialog:
	When a property is clicked, it opens the property edit dialog. Below the fields, show a special expandable section for Property Details. Show details about this property, and its valid values as well.
	The details should be in markdown format, and stored in a list of objects.
	Use https://github.com/showdownjs/showdown for parsing markdown (use npm save dev or something and copy the js and required files to static/js and use them accordingly)

4. Split sections in sections.html and layout.html into their own files and include them properly.
	split up app.js into different sub modules doing its own purpose.

5. Re-build everything to use web-sockets instead of polling for data, for real-time data transmission.
	Client - Server websocket connection. 

6. Allow renaming world name

add a kick option to each player in players list, which sends a kick command with the players name. 